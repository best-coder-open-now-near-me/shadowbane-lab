# Experiments

Generated search outputs belong under `experiments/results/` and are ignored by Git.
Commit small named fixtures only when they are useful for reproducibility or regression testing.

Every retained experiment should record:

- simulator commit SHA;
- ruleset version;
- seed;
- scenario/opponent league;
- evaluation budget;
- descriptor definitions;
- fitness definition.
