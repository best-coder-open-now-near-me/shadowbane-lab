# Combined movement repair candidate 1.7.2

Exact package source: `a98a6b370ffc3fdbaeadd82043513ed02a905fc1` on
`codex/native-lifecycle-hardening`; common batch base
`14d117e8c5194c6dff55dac608b2d3f683187d31`. Native product 1.7.2.0,
wheel 0.2.2; ABI and movement wire versions unchanged. Later documentation commits
are not the package source. Main and the installed VM client remain unchanged.

## Included and excluded work

Preserves the owner-accepted 1.7.1 selection highlight and existing navigation,
sky and shared runtime hardening. Adds movement owner
`69661e4e2811a02cccf9ba5c7f0701af14acc140` as `d98d666`, required package gates
`2db5beb`, version `416ccdd`, isolated archived-source helper fix `a98a6b3`,
and black PID popup source `56c8d9c`/`afb3b5b`.

Movement startup now accepts the exact reviewed bootstrap-prepared executable,
while retaining full original authentication, exact prepared loaded-code comparison
and update-function verification. Startup-hook failure publishes a readable
terminal-only status without inventing readiness, a window or a movement lease.
The reported settings failure occurred before controller input/focus handling.
The existing settings admission passed the production panel regression unchanged.

Particles scheduler `69d7717` remains excluded, paused and not runtime-wired;
visible particles remain suppressed. PvP identity branch `dcc050c` is separate,
not included. The deferred hand-material observation and ideal transparency
findings remain open; basic highlight appearance and demonstrated navigation
are not reopened by this repair.

## Exact artifacts

Local package directory: `E:/Projects/shadowbane/artifacts/combined-packages/616699a1`.

- ZIP `navigation-inspector-acceptance.zip`: `5c61a89c1b55d463497f24433784e6e255860c4ba2df2310c9963f16cf369082`
- Full DLL: `cb632795c928a57adf56873ff34eefee670f6d66ab5236c2e30c47c232001093`
- Diagnostics DLL: `9a6747125faf32020d94ab6090250d886360c665a1ce1e4c59e804cc697b6b20`
- Wheel `shadowbane_lab-0.2.2-py3-none-any.whl`: `dce430b7fc90acb286fef25af234303a68dad99ab2bc0041f8bf1f8e6f92a270`

All 57 receipt file hashes/sizes verified; ZIP CRC passed; no private prepared
executable is in the ZIP. Receipt and logs remain with the package. Failed initial
builder attempt `6fef409c` is retained as failure evidence, not a candidate: its
new helper lacked the archived source import path. The final builder starts fresh
from the fixed clean commit and passed that step for both profiles.

## Executed validation

- Full Python suite: 1833 passed, 11 skipped, 238 subtests passed; Ruff passed.
- Each native profile: 105 executed required CTests passed; 3 no-argument private
  binding cases skipped. Actual selected/sky bindings and prepared movement
  original-plus-prepared image verification then explicitly executed and passed.
- Each profile: 30 real movement IPC tests passed, zero skips, including terminal
  startup failure read by the Python consumer with no window or lease.
- Both profile runtime source-list checks passed through the existing builder.
- Installed wheel outside source: entry point, Graphics Lab, effects, selection,
  sky, movement button, conservative status and trace reader passed. Additional
  actual Tk check verified readonly field and popup/selected-popup text black.
- Existing ideal cue/effects transparency stretch diagnostics remain failed and
  explicitly deferred; they are not passed runtime gates or enabled particles.
- Independent Astra review by existing particles owner: no blocking findings in
  `2db5beb` and final delta through `a98a6b3`. Source/assertion review only; actual
  package execution and private executable checks are certified above by root.

Hosted CI [34043788119](https://github.com/best-coder-open-now-near-me/shadowbane-lab/actions/runs/34043788119): all seven jobs succeeded on the exact source.
Python 3.11/3.12/3.13 each executed 1837 passing tests, 4 skips and 241 passing
subtests. Both native logs explicitly show the startup-hook-failure and actual
settings-runtime cases executing and passing. Hosted graphics/private-data skips
do not replace the explicit local profile and private-image execution above.
Full CI log retained at `artifacts/combined-1.7.2-ci.log` in the integration worktree.

## Single remaining connected check

After applicable deployment authorization, use this exact full-profile package
with the existing reviewed bootstrap preparation and software-renderer launch
configuration. Preserve the current configuration and exact process ownership;
replace/restart only the designated diagnostic client, never unrelated instances.
Do not rerun a preparation helper that would overwrite a running client directory.

Confirm movement status is now published and the Movement controls button opens
settings. Then, in clear space, verify the enabled keyboard/controller adapter,
real stop on release, focus loss/chat safety and recovery, and isolation from
another client. Confirm the controller is visible inside the guest before treating
missing device input as an adapter failure. Briefly check manual/automation
handover using existing safe navigation; no broad navigation replay or PvP attack
is requested. Physical controller behavior and actual live movement startup remain
unaccepted until this connected check. Graphics appearance requires only a quick
non-regression observation if affected; accepted highlight behavior stays the baseline.

Next: coordinate the designated-client installation
and targeted connected check. No owner restart is needed before installation.
