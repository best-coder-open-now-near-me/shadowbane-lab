# Guard menu response correction: 1.8.11 / 0.3.20

Branch: codex/guard-upgrades. Exact packaged source: c1472a6 (pushed).
Integration destination: codex/vendor-rolling, then codex/native-lifecycle-hardening,
then reviewed main. This source is not merged; the normal main checkout is clean.

## Behavior and live finding

Warehouse building selection and the owned Seneschal roster were verified live.
Resource opening declined outside ordinary interaction range. After repositioning,
a building response was observed after the old ten-second native response window.
The unresolved request remains retained. No agent gold transfer or guard upgrade
was submitted in this qualification.

Menu navigation now waits up to 60 seconds natively, with 65 seconds for the host
to observe the terminal result. Both discovery and the funding traversal use the
menu budget. Each menu still has one durable request; there is no retry after
uncertain submission. Scene replacement, timeout and native uncertainty still
block later commands. Gold transfer and upgrade response limits are unchanged.

## Validation and activation

- Isolated source suite: 2,675 passed, 18 skipped; whole-source Ruff passed.
- Required Win32 native, real IPC and reviewed executable binding gates passed
  for both profiles.
- Installed wheel checks and six extra installed guard/native comparisons passed.
- All 61 package artifacts and the archive were independently hash verified.
- Guest preparation and read-only updater validation passed, with the current
  game executable and historical crafting records unchanged.
- Two pre-existing stretch transparency diagnostics remain unresolved per profile.
  This package does not claim whole-product visual or live guard acceptance.

The verified full DLL and host package are activated after confirmed closure.
The existing dashboard shortcut selects host 0.3.20; the same game shortcut launched
the verified full 1.8.11 extension. Loaded artifact, matching process lifetime,
read-only action mapping and healthy guard-capable manager checks passed.
Activation preserved historical records and did not submit game actions.
Local ignored artifacts retain exact deployment steps, hashes, rollback data
and private request evidence.

## Current live qualification

The corrected runtime opened the warehouse building and verified the owned
Seneschal roster. Resource opening still returned a definite unavailable response
while the player and exact NPC were nearby in the same parent. No uncertain
request was retried, no gold moved, and no upgrade was submitted.

Live follow-up: the user opened View Resources. The owned warehouse panel and
its exact NPC reference remained active while automatic navigation opened the
guard tower and read its complete roster. No gold moved and no upgrade was
submitted. This establishes the source for automatic return visits; the spatial
building query is unsuitable for this NPC. The correction is tracked in
[warehouse return ownership](guard-upgrades-1.8.12.md).
