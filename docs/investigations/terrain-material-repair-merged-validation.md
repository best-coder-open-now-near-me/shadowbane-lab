# Terrain material repair merged-head validation

This file intentionally triggers the ordinary repository validation matrix after
the exact-client terrain repair was merged into `codex/client-convergence-v2`.
It does not claim live visual acceptance. The acceptance build must come from the
same merged commit lineage after both native profiles, Python tests, quality
checks, PowerShell validation, and package production complete successfully.
